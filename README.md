# research-platform-releases

Public artifacts only. Source lives in the private `dorigabay/research-platform` repository.

- `update.json` is the machine-readable latest shell (and later app packs).
- GitHub Releases use tags `shell/vX.Y.Z` for installer assets.

Do not develop here.
